export default function AdminPage() {
  return (
    <main className="mx-auto max-w-5xl px-lg py-3xl">
      <section className="space-y-sm rounded-lg border border-border bg-surface p-xl shadow-card">
        <p className="text-sm font-medium uppercase tracking-[0.14em] text-brand">
          Admin
        </p>
        <h1 className="font-heading text-3xl font-semibold text-text-strong">
          Admin placeholder
        </h1>
        <p className="max-w-2xl text-base text-text-soft">
          This route group remains in place, but its content is intentionally minimal
          until the admin feature set is defined.
        </p>
      </section>
    </main>
  );
}
